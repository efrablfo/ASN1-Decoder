/**
 * This class file was automatically generated by jASN1 v1.9.0 (http://www.openmuc.org)
 */

package chargingcdr;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class RatedUnitsValues implements Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private Unsigned32 ratedTimeUnits = null;
	private Unsigned64 ratedTotalOctetsUnits = null;
	private Unsigned64 ratedUplinkOctetsUnits = null;
	private Unsigned64 ratedDownlinkOctetsUnits = null;
	private Unsigned64 ratedServiceSpecificUnits = null;
	
	public RatedUnitsValues() {
	}

	public RatedUnitsValues(byte[] code) {
		this.code = code;
	}

	public void setRatedTimeUnits(Unsigned32 ratedTimeUnits) {
		this.ratedTimeUnits = ratedTimeUnits;
	}

	public Unsigned32 getRatedTimeUnits() {
		return ratedTimeUnits;
	}

	public void setRatedTotalOctetsUnits(Unsigned64 ratedTotalOctetsUnits) {
		this.ratedTotalOctetsUnits = ratedTotalOctetsUnits;
	}

	public Unsigned64 getRatedTotalOctetsUnits() {
		return ratedTotalOctetsUnits;
	}

	public void setRatedUplinkOctetsUnits(Unsigned64 ratedUplinkOctetsUnits) {
		this.ratedUplinkOctetsUnits = ratedUplinkOctetsUnits;
	}

	public Unsigned64 getRatedUplinkOctetsUnits() {
		return ratedUplinkOctetsUnits;
	}

	public void setRatedDownlinkOctetsUnits(Unsigned64 ratedDownlinkOctetsUnits) {
		this.ratedDownlinkOctetsUnits = ratedDownlinkOctetsUnits;
	}

	public Unsigned64 getRatedDownlinkOctetsUnits() {
		return ratedDownlinkOctetsUnits;
	}

	public void setRatedServiceSpecificUnits(Unsigned64 ratedServiceSpecificUnits) {
		this.ratedServiceSpecificUnits = ratedServiceSpecificUnits;
	}

	public Unsigned64 getRatedServiceSpecificUnits() {
		return ratedServiceSpecificUnits;
	}

	public int encode(OutputStream os) throws IOException {
		return encode(os, true);
	}

	public int encode(OutputStream os, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				os.write(code[i]);
			}
			if (withTag) {
				return tag.encode(os) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (ratedServiceSpecificUnits != null) {
			codeLength += ratedServiceSpecificUnits.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 4
			os.write(0x84);
			codeLength += 1;
		}
		
		if (ratedDownlinkOctetsUnits != null) {
			codeLength += ratedDownlinkOctetsUnits.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			os.write(0x83);
			codeLength += 1;
		}
		
		if (ratedUplinkOctetsUnits != null) {
			codeLength += ratedUplinkOctetsUnits.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			os.write(0x82);
			codeLength += 1;
		}
		
		if (ratedTotalOctetsUnits != null) {
			codeLength += ratedTotalOctetsUnits.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			os.write(0x81);
			codeLength += 1;
		}
		
		if (ratedTimeUnits != null) {
			codeLength += ratedTimeUnits.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			os.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(os, codeLength);

		if (withTag) {
			codeLength += tag.encode(os);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			ratedTimeUnits = new Unsigned32();
			subCodeLength += ratedTimeUnits.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			ratedTotalOctetsUnits = new Unsigned64();
			subCodeLength += ratedTotalOctetsUnits.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			ratedUplinkOctetsUnits = new Unsigned64();
			subCodeLength += ratedUplinkOctetsUnits.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			ratedDownlinkOctetsUnits = new Unsigned64();
			subCodeLength += ratedDownlinkOctetsUnits.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 4)) {
			ratedServiceSpecificUnits = new Unsigned64();
			subCodeLength += ratedServiceSpecificUnits.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream os = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(os, false);
		code = os.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (ratedTimeUnits != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ratedTimeUnits: ").append(ratedTimeUnits);
			firstSelectedElement = false;
		}
		
		if (ratedTotalOctetsUnits != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ratedTotalOctetsUnits: ").append(ratedTotalOctetsUnits);
			firstSelectedElement = false;
		}
		
		if (ratedUplinkOctetsUnits != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ratedUplinkOctetsUnits: ").append(ratedUplinkOctetsUnits);
			firstSelectedElement = false;
		}
		
		if (ratedDownlinkOctetsUnits != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ratedDownlinkOctetsUnits: ").append(ratedDownlinkOctetsUnits);
			firstSelectedElement = false;
		}
		
		if (ratedServiceSpecificUnits != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ratedServiceSpecificUnits: ").append(ratedServiceSpecificUnits);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

